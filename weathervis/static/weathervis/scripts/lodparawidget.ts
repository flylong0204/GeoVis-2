/// <reference path="weathervislib.d.ts" />

module WeatherVis {
    export class LodParaWidget {        
        constructor(private parentDivId: string, px: number, py: number, w: number, h: number) {
            
        } 
    }
}