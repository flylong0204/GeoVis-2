var Core;
(function (Core) {
    // property names: color, radius, etc.
    var Property = (function () {
        function Property() {
        }
        return Property;
    })();
    Core.Property = Property;
})(Core || (Core = {}));
